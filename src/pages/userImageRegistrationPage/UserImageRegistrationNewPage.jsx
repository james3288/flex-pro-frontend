import React from "react";
import DashboardPage from "../DashboardPage";
import { PageName } from "../../constants/enum";

const UserImageRegistrationNewPage = ({ page }) => {
  return <DashboardPage dashboardBg="userImageRegistrationNew" page={page} />;
};

export default UserImageRegistrationNewPage;
